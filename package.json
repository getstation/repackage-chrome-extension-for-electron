{
  "name": "@getstation/repackage-chrome-extension-for-electron",
  "version": "0.1.1",
  "description": "Repackage a Chrome extension to be loaded via `addDevToolsExtension`",
  "main": "index.js",
  "bin": {
    "repackage-chrome-extension": "./cli.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alexandre Lacheze",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.5.0",
    "fs-extra": "^3.0.1"
  }
}
